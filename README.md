# This repo is made public for resume purposes

This repo is still in development, to run the web server, do the following:

```bash
cargo run
```

To see a legacy version of the [website](https://werdxz.info).
